# ===========================================
# SocialNetworkBe Environment Variables
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit the actual .env file to git!

# ===========================================
# DATABASE - Azure SQL Server
# ===========================================
# Your Azure SQL Server: ngohai.database.windows.net
# Replace <database-name>, <username>, and <password> with your actual values

ConnectionStrings__MyDb=Server=tcp:ngohai.database.windows.net,1433;Initial Catalog=<database-name>;Persist Security Info=False;User ID=<username>;Password=<password>;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;

# ===========================================
# JWT Authentication
# ===========================================
# Generate a secure 256-bit key (64 hex characters)
# You can generate one at: https://generate-random.org/api-key-generator
JWT__Key=YOUR_SECRET_KEY_MIN_32_CHARACTERS_LONG_HERE_CHANGE_THIS
JWT__Issuer=https://your-app.up.railway.app
JWT__Audience=https://your-app.up.railway.app

# ===========================================
# Cloudinary (Image/Video Upload)
# ===========================================
# Format: cloudinary://API_KEY:API_SECRET@CLOUD_NAME
Cloudinary__Api=cloudinary://YOUR_API_KEY:YOUR_API_SECRET@YOUR_CLOUD_NAME

# ===========================================
# CORS - Allowed Origins
# ===========================================
# Your frontend URL(s) - separate multiple with comma
AllowedOrigins=https://your-frontend.vercel.app,http://localhost:3000

# ===========================================
# ASP.NET Core Settings
# ===========================================
ASPNETCORE_ENVIRONMENT=Production

# ===========================================
# RAILWAY DEPLOYMENT NOTES
# ===========================================
# On Railway, add these variables in:
# Dashboard -> Your Project -> Variables
#
# Required variables:
# 1. ConnectionStrings__MyDb  (Azure SQL connection string)
# 2. JWT__Key                 (Secret key for JWT)
# 3. JWT__Issuer              (Your Railway app URL)
# 4. JWT__Audience            (Your Railway app URL)
# 5. AllowedOrigins           (Your frontend URLs)
# 6. Cloudinary__Api          (Cloudinary API URL)
